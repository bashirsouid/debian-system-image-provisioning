[Content]
# Signal Desktop. The apt source + key fetcher are wired up in:
#   mkosi.profiles/signal/apt-keys.conf
#   mkosi.profiles/signal/mkosi.extra/etc/apt/sources.list.d/signal.sources
#
# Before enabling, pin the verified Signal signing-key fingerprint in
# apt-keys.conf (currently REPLACE_VERIFY_FROM_SIGNAL_DOCS), then
# uncomment Packages= below.
#
# Packages=
#     signal-desktop
