[Content]
# No packages required. This profile creates a systemd oneshot service
# that symlinks /var/lib/docker -> /mnt/data/docker on first boot,
# keeping container volumes on the persistent DATA partition so they
# survive A/B root swaps.