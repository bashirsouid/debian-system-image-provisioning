[Content]
# k3s isn't shipped as a native Debian package — upstream uses its own
# install script (get.k3s.io). Options:
#   - bundle the install binary under mkosi.profiles/k3s/mkosi.extra/usr/local/bin/
#     and a systemd unit under mkosi.extra/etc/systemd/system/k3s.service
#   - run the installer as a first-boot one-shot (network required).
#
# Packages=
#     # deps once approach picked
