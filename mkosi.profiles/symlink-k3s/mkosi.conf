[Content]
# No packages required. This profile creates a systemd oneshot service
# that symlinks K3s and container storage directories to /mnt/data/ on
# first boot, keeping persistent state across A/B root swaps.