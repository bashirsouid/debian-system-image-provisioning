[Content]
# Server baseline: minimal headless CLI tooling. Remote access
# (ssh-server, tailscale, cloudflare-tunnel) and monitoring
# (healthchecksio) live in their own profiles — compose with:
#   server ssh-server tailscale cloudflare-tunnel healthchecksio
Packages=
    net-tools
    iptables
