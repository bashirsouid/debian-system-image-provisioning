[Content]
# Cloud backup service - hourly Kopia snapshots to cloud storage.
# NOTE: Requires the kopia profile to be selected alongside this profile.
# Bundle both with: --profile=backup (the backup role includes kopia, cloud-backup, home-server-backup)
Packages=
# This profile only provides the timer/service units. The kopia-backup-trigger
# script and CLI are installed by the kopia profile.