[Content]
# Flatpak runtime + a first-boot one-shot that adds the Flathub remote
# (mkosi.extra/etc/systemd/system/flatpak-flathub-init.service) so
# `flatpak install flathub <app>` works out of the box. Other flatpak
# remotes can be added later interactively or via ansible.
Packages=
    flatpak
