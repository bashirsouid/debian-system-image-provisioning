[Content]
# Flatpak runtime + a retrying timer that adds the Flathub remote once
# DNS/network are actually ready. Other flatpak remotes can be added later
# interactively or via ansible.
Packages=
    flatpak
