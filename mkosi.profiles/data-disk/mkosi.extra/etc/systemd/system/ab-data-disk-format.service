[Unit]
Description=Format OCI data disk (idempotent, retries every boot)

[Service]
Type=oneshot
RemainAfterExit=yes
TimeoutStartSec=0
ExecStart=/usr/local/sbin/ab-data-disk-format

[Install]
WantedBy=multi-user.target
