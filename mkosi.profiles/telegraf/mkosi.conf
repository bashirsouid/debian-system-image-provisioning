[Content]
# Telegraf metrics agent. Apt source + key fetcher wired up in:
#   mkosi.profiles/telegraf/apt-keys.conf
#   mkosi.profiles/telegraf/mkosi.extra/etc/apt/sources.list.d/influxdata.sources
#
# Before enabling, pin the verified InfluxData signing-key fingerprint
# in apt-keys.conf (currently REPLACE_VERIFY_FROM_INFLUXDATA_DOCS),
# then uncomment Packages= below.
#
# Packages=
#     telegraf
