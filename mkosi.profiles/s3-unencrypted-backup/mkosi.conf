[Content]
# S3 Unencrypted Backup Profile
# Uploads configured files to S3-compatible storage on an hourly schedule.
# Requires: s3-backup-credentials.json secret (see .mkosi-secrets.example/)
Packages=
    mc
    openssl
    jq
