[Content]
# Zoom is installed by a first-boot oneshot because upstream only
# publishes a moving "latest" .deb URL and mkosi build networking is
# not always available. Keep this profile out of Packages= merging;
# an empty Packages= assignment resets the inherited package list.
