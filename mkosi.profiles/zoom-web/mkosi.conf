[Content]
# Zoom is installed during the image build by mkosi.postinst. Upstream only
# publishes a moving "latest" .deb URL, so the build fails loudly if that URL
# is unavailable instead of flashing an image with a first-boot installer.
