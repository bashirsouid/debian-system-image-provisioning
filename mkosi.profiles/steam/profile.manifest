description="Steam client (Debian steam-installer + i386 GL/Vulkan stack). The profile's mkosi.prepare enables i386 multilib in the build chroot before apt-get install resolves :i386 deps."
uses_secrets=""
