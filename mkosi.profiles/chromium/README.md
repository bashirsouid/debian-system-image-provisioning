# Chromium

This profile installs **Chromium**, the open-source foundation for Chrome. It provides a fast, standards-compliant web browser with built-in privacy features.

## Usage

Add the profile to a host descriptor (e.g., `hosts.local/example-host.conf`):

```ini
profiles = ... chromium
```
