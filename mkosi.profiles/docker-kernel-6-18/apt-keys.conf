# apt-keys.conf for the docker-kernel-6-18 profile.
#
# Fetches the Docker signing key into the profile's mkosi.extra/
# so the Docker apt repository can be verified during package install.
#
# For format details see mkosi.profiles/docker/apt-keys.conf
KEY_1_NAME="docker-archive-keyring"
KEY_1_URL="https://download.docker.com/linux/debian/gpg"
KEY_1_FINGERPRINT="9DC858229FC7DD38854AE2D88D81803C0EBFCD88"
KEY_1_OUT="etc/apt/keyrings/docker.gpg"