[Match]
Architecture=x86-64

[Content]
# Meta-packages, pinned to trixie-backports in
# mkosi.extra/etc/apt/preferences.d/kernel-rolling.pref. apt resolves
# these to the newest backports kernel available at build time.
Packages=
    linux-image-amd64
    linux-headers-amd64
