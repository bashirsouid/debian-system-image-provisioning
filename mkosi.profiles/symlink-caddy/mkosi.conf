[Content]
# symlink-caddy = persist Caddy TLS certs/ACME account across A/B swaps.
# This profile ships ONLY the symlink oneshot service; no packages.
# The Caddy package (from k3s-proxy) creates the 'caddy' user at build time.