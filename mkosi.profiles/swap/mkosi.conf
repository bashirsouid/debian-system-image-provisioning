# Swap setup profile - creates a 2GiB swap file on first boot
# This profile is non‑destructive to partition layout.
[Content]
# No extra packages needed – util‑linux (mkswap/swapon) is part of the base image.
