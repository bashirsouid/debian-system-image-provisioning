description="Repo-only base: Brave apt source + signing key. Pulled in automatically (requires=) by brave-browser / brave-origin, which install their respective packages from it; not selected directly."
uses_secrets=""
