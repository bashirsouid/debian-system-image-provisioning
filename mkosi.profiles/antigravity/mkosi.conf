[Content]
# Google Antigravity is currently distributed as an AppImage or Flatpak,
# not an apt package. Options:
#   - pair with 'flatpak' profile and flatpak install via a first-boot
#     one-shot, OR
#   - drop an AppImage under mkosi.profiles/antigravity/mkosi.extra/usr/local/bin/
#     and a .desktop file.
#
# Packages=
#     # add deps here once install path is picked
