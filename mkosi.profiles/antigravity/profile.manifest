description="Google Antigravity suite (CLI + IDE + Hub). Installed via a first-boot oneshot from antigravity.google so the user always gets the latest standalone releases, which then self-update."
uses_secrets=""
