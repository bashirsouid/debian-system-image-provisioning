description="SFTP-only server with sftponly user. Uses SSH internal-sftp, restricts user to file transfer only with no shell access. Home directory at /home/sftponly with /home/sftponly/uploads as writeable chroot."
uses_secrets=""
