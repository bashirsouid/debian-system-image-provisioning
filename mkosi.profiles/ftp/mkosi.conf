[Content]
# vsftpd is a minimal FTP server. Pair with a drop-in under
# mkosi.profiles/ftp/mkosi.extra/etc/vsftpd/ if you need non-default
# config. Consider SFTP (via openssh-server's sftp-server) instead if
# this is just for file transfer.
Packages=
    vsftpd
