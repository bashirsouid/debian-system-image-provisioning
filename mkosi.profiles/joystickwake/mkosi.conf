[Content]
# joystickwake - prevents DPMS timeout on controller input
#
# The joystickwake upstream source is cloned and built during mkosi's build
# phase. Required Python runtime dependencies are listed below.

Packages=
    python3
    python3-pyudev
    python3-setuptools
    python3-dbus-next
    python3-xlib