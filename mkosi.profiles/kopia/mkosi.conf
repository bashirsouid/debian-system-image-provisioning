[Content]
# Kopia backup. Apt source + key fetcher wired up in:
#   mkosi.profiles/kopia/apt-keys.conf
#   mkosi.profiles/kopia/mkosi.extra/etc/apt/sources.list.d/kopia.sources
#
# Before enabling, pin the verified Kopia signing-key fingerprint in
# apt-keys.conf (currently REPLACE_VERIFY_FROM_KOPIA_DOCS), then
# uncomment Packages= below.
#
# Packages=
#     kopia
