[Content]
# Kopia backup. Apt source + signing-key fingerprint pinning are
# already wired up in:
#   mkosi.profiles/kopia/apt-keys.conf
#   mkosi.profiles/kopia/mkosi.extra/etc/apt/sources.list.d/kopia.sources
#
# To enable: uncomment Packages= below and add 'kopia' to a host's
# profile.default. update-3rd-party-deps.sh will fetch + verify the
# Kopia signing key on the next build.
#
# Packages=
#     kopia
