[Content]
# Microsoft VSCode. The apt source + key fetcher are wired up in:
#   mkosi.profiles/vscode/apt-keys.conf
#   mkosi.profiles/vscode/mkosi.extra/etc/apt/sources.list.d/vscode.sources
#
# Before adding vscode to a host's profile.default, pin the Microsoft
# signing-key fingerprint in apt-keys.conf (it currently says
# REPLACE_VERIFY_FROM_MS_DOCS so update-3rd-party-deps.sh skips it
# loudly), then uncomment Packages= below.
#
# Packages=
#     code
