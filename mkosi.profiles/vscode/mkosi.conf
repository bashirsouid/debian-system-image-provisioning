[Content]
# Microsoft VSCode. Apt source + signing-key fingerprint pinning are
# already wired up in:
#   mkosi.profiles/vscode/apt-keys.conf
#   mkosi.profiles/vscode/mkosi.extra/etc/apt/sources.list.d/vscode.sources
#
# To enable: uncomment Packages= below and add 'vscode' to a host's
# profile.default. update-3rd-party-deps.sh will fetch + verify the
# Microsoft signing key on the next build.
#
# Packages=
#     code
