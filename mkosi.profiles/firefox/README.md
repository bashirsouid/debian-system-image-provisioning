# Firefox

This profile installs **Firefox ESR** (Extended Support Release), Mozilla's stable, privacy-focused web browser with long-term support and security updates.

## Usage

Add the profile to a host descriptor (e.g., `hosts.local/example-host.conf`):

```ini
profiles = ... firefox
```
