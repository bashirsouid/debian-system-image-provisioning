# Copy this file to .env and change the values.
# .env must NOT be committed to git.

INITIAL_USERNAME=demo
INITIAL_PASSWORD=change-me-now
INITIAL_GROUPS=sudo,audio,video,render,input
INITIAL_SHELL=/bin/bash
