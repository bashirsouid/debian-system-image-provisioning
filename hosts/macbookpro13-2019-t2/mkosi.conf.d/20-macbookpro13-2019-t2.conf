[Match]
Profiles=macbook

# Host-level overrides for this specific MacBook Pro 13" 2019 (T2) go
# here. At the moment the T2 kernel + firmware + initramfs-modules live
# in the macbook profile and the audio stack lives in audio-pipewire,
# so this file is a placeholder — kept so future host-only tweaks
# (e.g. a sensor-specific kernel cmdline, a hostname-embedded
# nftables rule) have a canonical home.

[Content]
Packages=
