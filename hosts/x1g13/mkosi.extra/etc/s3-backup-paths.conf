# S3 Backup Paths for x1g13
# This is a test file - backs up this very file to demonstrate the mechanism
#
# Format: One path per line (files, directories, or glob patterns)

# Upload this paths file itself (self-referential test)
/etc/s3-backup-paths.conf

# Example: upload SSH host keys (already public, safe to backup)
# /etc/ssh/ssh_host_*.pub

# Example: upload trusted firmware files 
# /usr/lib/firmware/intel/ibt-0190-0041*.sfi