This directory is only seeded for QEMU test runs started through ./run.sh.

It is meant to give a small, known-good AwesomeWM setup for smoke testing.
It will not overwrite an existing ~/.config/awesome directory.
