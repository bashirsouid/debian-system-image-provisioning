Drop executable health-check scripts in this directory when you want slot
promotion to depend on application-specific checks.

Each executable file is run by `ab-slot-health.service` after the built-in
"no failed systemd units" check. A non-zero exit code marks the slot unhealthy.
