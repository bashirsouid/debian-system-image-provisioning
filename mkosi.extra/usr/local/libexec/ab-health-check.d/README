Additional boot-health hook scripts may be placed in this directory.

Each executable file is run by `ab-health-gate.service` after the built-in
"no failed units" check and before `boot-complete.target` is reached.

Return status:
- exit 0 -> hook passed
- non-zero -> hook failed and the boot is not considered successful
